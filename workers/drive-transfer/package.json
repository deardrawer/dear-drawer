{
  "name": "guest-drive-transfer-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Queue consumer: R2 → Google Drive resumable transfer for guest photo sharing",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260124.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.60.0"
  }
}
